# EPIC45: 設定画面 UI/UX リファイン

## 目的
肥大化した設定画面（`GlobalSettingsModal.tsx` 1536行 / 5タブ）を、左サイドバー型のマスターディテールUIに再構築し、情報の発見性・保守性・視覚的一貫性を向上させる。

## 背景
- Epic39〜43 で設定項目（CLI検出、Team設定、追加APIプロバイダー、POアシスタントCLIサポート、Transport統一）が段階的に追加され、設定画面が肥大化。
- 現状の水平タブバー構成では、ヘッダーが散らばり設定項目の階層が把握しづらい。
- POアシスタントタブだけで約1150行あり、単一コンポーネントとして保守性が低下。

## タスクリスト

### Phase 1: 基盤整備
- [x] `src/components/ui/settings/` ディレクトリを新設
- [x] `SettingsContext.tsx` を作成（state/save/reset を集約）
- [x] `SettingsShell.tsx` を作成（マスターディテールレイアウト）
- [x] `SettingsSidebar.tsx` を作成（カテゴリ＆セクションナビ）
- [x] `SettingsSection.tsx` を作成（右ペイン共通ラッパー）
- [x] `SettingsField.tsx` を作成（ラベル/説明/入力統一）

### Phase 2: セクション実装
- [x] `sections/ProjectSection.tsx` — プロジェクト設定（パス、Danger Zone）
- [x] `sections/PoAssistantSection.tsx` — Visual Identity / Execution Mode
- [x] `sections/AiProviderSection.tsx` — Provider/APIキー/モデル選択
- [x] `TeamSettingsTab.tsx` を `SettingsSection` ラッパー内で利用可能に調整
- [x] `SetupStatusTab.tsx` を同様に調整
- [x] `AnalyticsTab.tsx` を同様に調整

### Phase 3: クイックスイッチャーの実装
- [x] POアシスタント画面（サイドバー等）に、現在のプロバイダーとモデルを切り替えられる軽量なUI（ドロップダウン等）を追加する
- [x] Inception Deck画面にも同様の切り替えUIを追加する

### Phase 4: 統合
- [x] 設定画面本体を `SettingsShell` ベースへ統合し、独立画面へ移行できる構造にする
- [x] 情報アーキテクチャを 3カテゴリ / 6セクション に再編
  - 一般: プロジェクト
  - AI & モデル: POアシスタント / AIプロバイダー / チーム設定
  - システム: セットアップ状況 / アナリティクス
- [x] レスポンシブ折りたたみ対応（`md:` 以下でドロワー化）

### Phase 5: 検証
- [x] 既存設定項目すべてが新UIから到達可能か
- [x] 保存・永続化の動作確認（Tauri Store）
- [x] 各プロバイダー切替・モデル取得動作確認
- [x] Team設定のCRUD動作確認
- [x] レスポンシブ切替確認
- [x] `frontend-core` 配下に差分がないことを `git diff` で確認
- [x] walkthrough.md に結果記録

### Phase 6: 独立設定画面とナビゲーション再設計
- [x] 設定画面をモーダルではなく独立したアプリ画面として表示する
- [x] 左上ハンバーガーメニューから Kanban / Inception Deck / 設定 に遷移できるナビゲーションを追加する
- [x] 設定の並び順をユーザーの操作フロー基準へ再構成する
- [x] APIキー設定と「どのプロバイダー / モデルを使うか」の選択 UI を明確に分離する

### Phase 7: 設計意図ズレの修正（v2 リファイン改訂）
> 背景: Phase 6 までの実装でナビ・設定導線・視覚デザインがユーザーの設計意図とズレたため、以下を再実装する。**計画段階。着手前にユーザー承認を得ること**。

#### Z-1. ヘッダー再構成
- [x] ハンバーガーメニュー（左上）を撤去する
- [x] Current View 読み取り専用表示（`Kanban / Inception Deck / 設定`）を撤去する
- [x] ヘッダー中央に Kanban / Inception Deck のセグメント切替を新設（クリックで `currentView` 切替、設定 view は含めない）
- [x] POアシスタントボタン（ヘッダー右上）を撤去（エッジタブへ移行）
- [x] Scaffold 関連の常設ボタンが残っていれば撤去し、Inception Deck Phase5 内に集約する
- [x] `LlmUsagePill` を集約数値 1 つ + ホバー詳細に縮小（**ユーザー確認済み**）
  - デフォルト表示: 合計トークン数 + 合計コスト（`¥xxx` または `$x.xxx`）1 行のみ
  - ホバー時: Project / Sprint 別の内訳を tooltip で表示
  - アイコンは `lucide-react` の `Coins` など単色 1 つに統一

#### Z-2. エッジタブハンドル導線の統一
- [x] `src/components/ui/EdgeTabHandle.tsx` を新規作成
  - props: `side: "left" | "right" | "bottom"`, `icon`, `label`, `active`, `onClick`
  - 縦/横配置とラベル回転を内部で処理
- [x] 左端縦タブ: ⚙ 設定 → 設定ドロワー開閉
- [x] 右端縦タブ: POアシスタント → 右サイドパネル開閉（既存トグルを置換）
- [x] 下端横タブ: チームの稼働状況 → ターミナルドック開閉（既存トグルを置換）
- [x] パネル展開時はタブが「閉じる」ハンドル状態に変化
- [x] ヘッダー側の対応ボタンは Z-1 と合わせて削除

#### Z-3. 設定ドロワー化（view swap → overlay drawer）
- [x] `App.tsx` の `currentView === "settings"` 分岐を撤去し、`isSettingsOpen` state で制御
- [x] `SettingsPage.tsx` を「左からスライドインするオーバーレイ型ドロワー」に変更（**ユーザー確認済み**）
  - 幅: 520px 固定（ユーザー確認済み）
  - `position: fixed` で左端から表示、メインコンテンツのレイアウトには影響を与えない
  - 背後にスクリム（`bg-slate-900/40 backdrop-blur-sm`）を敷く
  - スクリムクリック / Esc / ×ボタン / 左端タブ再クリック で閉じる
  - `translate-x` + `transition-transform duration-200` でスライドイン
- [x] `SettingsShell` 内部の 2 ペインレイアウトを維持（ナビ 180px / 詳細 flex-1）
- [x] 小幅時はナビをアコーディオン化（既存のドロワー折りたたみロジックを活用）
- [x] 保存ボタンは詳細ペイン下部に固定

#### Z-4. 視覚ノイズ削減（全セクション横断スタイル統一）
- [x] 共通スタイルトークンをコメントで `SettingsShell.tsx` 先頭に明記（実装指針）
- [x] プロバイダー色分け（sky/violet/orange/cyan）を**廃止**、アイコンのみモノクロで識別
- [x] `AiProviderSection.tsx`（428 行）のカード群をニュートラルベースに書き換え
- [x] `PoAssistantSection.tsx` のグラデーション背景（blue-50/70 + emerald-50/70）を撤去
- [x] 角丸 `rounded-2xl` → `rounded-xl` に統一
- [x] パディング: カード `p-5` / 内部 `p-4` / セクション間 `space-y-6` に統一
- [x] 選択状態は `ring-2 ring-blue-500` のみ使用
- [x] セマンティック色: 赤=危険 / 琥珀=注意 / 翠=成功 のみ。情報表示にカラーバッジを使わない
- [x] タイポ階層: タイトル `text-lg font-semibold text-slate-900` / 説明 `text-sm text-slate-500`
- [x] `SetupStatusTab.tsx` / `TeamSettingsTab.tsx` / `AnalyticsTab.tsx` にも同ルール適用

#### Z-5. AiQuickSwitcher のスタイル整合
- [x] `AiQuickSwitcher.tsx` を Z-4 のスタイルルールに準拠させる（機能は維持）

#### Z-6. 検証
- [x] ヘッダーからハンバーガー / 現在view表示 / PO ボタン / Scaffold ボタンが消えている
- [x] ヘッダー中央のセグメントで Kanban ↔ Inception Deck が切替可能
- [x] 左端タブで設定ドロワーが開閉し、メインがプッシュされる
- [x] 右端タブで PO アシスタント、下端タブでチームの稼働状況が開閉
- [x] 設定各セクションで多色（sky/violet/orange/cyan/gradient）が一掃されている
- [x] `frontend-core` (`src/context/**`, `src/hooks/**`, `src/types/**`) に差分なし
- [x] `npm run tauri dev` で起動確認

### Phase 8: 導線とハンドル表現の追加調整
- [x] 追加要件を `task.md` / `implementation_plan.md` に反映する
- [x] POアシスタントのエッジタブハンドル文字向きを修正し、ほんのり色付きの視認しやすい見た目に整える
- [x] POアシスタントを開いたとき、エッジタブハンドルがパネル境界に追従するように調整する
- [x] 設定導線を左端タブではなく右上の歯車ボタン経由の独立ページへ戻し、設定画面の中身は維持する

### Phase 9: ビュー切り替え導線の再整理
- [x] ヘッダー中央のセグメントを `Inception Deck / Kanban / Settings` の順に再編し、設定導線をここへ統合する

### Phase 10: 設定セクション配置レビューの反映
- [x] セットアップ状況の補足注釈を 1 つに統合し、画面下部ではなく自然な位置へ移動する
- [x] 利用するAIの補足注釈を 1 つに統合し、画面下部ではなく自然な位置へ移動する
- [x] APIキー / 接続情報の補足注釈とプロバイダーカード配置を整理し、1列レイアウトへ最適化する
- [x] POアシスタント設定の画像・APIモード・CLIモードの配置を見直し、間延びを解消する
- [x] チーム設定の初期並行数を 5 に変更し、テンプレートカード配置を最適化した上で既知課題を `BACKLOG.md` に記録する

### Phase 11: POアシスタント設定レイアウトの再調整
- [x] POアシスタント設定を「上段: 実行モードカード / 下段: 実行詳細 + 画像パネル」に再配置し、要素を減らさずに情報整理する

### Phase 12: UIレビュー反映（統合整理）
- [x] 「利用するAI」と「APIキー / 接続情報」を統合し、「AIプロバイダー設定」として選択と接続情報を一体化する
- [x] POアシスタント設定を「左: 実行設定 / 右: 見た目」の目的別2カラム構成へ再整理し、Current 表示やカード階層を見直す

### Phase 13: チーム設定UIレビュー反映
- [x] チーム設定の巨大な「Model References」セクションを縮小し、ロール設定内の補助操作へ統合する
- [x] 各ロールカードを「基本情報 → エンジン設定 → システムプロンプト」の縦ストリームへ再構成する
- [x] 複数ロールをアコーディオン形式で扱い、間延びしにくい編集体験へ整える

### Phase 14: チーム設定のマスターディテール再設計
- [x] チーム設定のロール編集領域を `300px : 1fr` 目安の2カラムマスターディテール構成へ再編する
- [x] 左カラムをカード型のロールデータリストとして整え、最下部に「＋ ロールを新規追加」導線を配置する
- [x] 右カラムを単一の詳細カードに集約し、「アイデンティティ → エンジン設定 → システムプロンプト」の流れへ整理する

### Phase 15: ロール一覧カードの簡素化
- [x] ロール一覧カードを「アバター / 役職名 / CLI / モデル」のみのコンパクト表示へ調整する

### Phase 16: AIプロバイダー設定の接続情報専用化
- [x] AIプロバイダー設定からラジオボタンと選択中の青枠表現を撤去する
- [x] AIプロバイダー設定を APIキー / 接続情報の入力専用画面へ整理する

### Phase 17: エッジタブハンドルの視認性調整
- [x] POアシスタント / Devコンソールのエッジタブハンドルを、ソリッドなエレベーショングレー基調の見た目へ調整する

### Phase 18: エッジタブ文言調整
- [x] 下端エッジタブの「DEVエージェントバー」表記を「チームの稼働状況」へ変更する

## 完了条件
- 設定画面が左サイドバー構成で動作する
- 既存の全設定項目・保存挙動が維持されている
- `src/context/**`, `src/hooks/**`, `src/types/**` に変更が入っていない
- `npm run tauri dev` でエラーなく起動する

## スコープ外
- 設定項目の追加・削除・リネーム
- バックエンド (Rust) 変更
- Tauri Store キー名・スキーマ変更
- 検索バー / 未保存バッジ / キーボードナビ（ユーザー確認済みで今回スコープ外）
