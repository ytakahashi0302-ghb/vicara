# MicroScrum AI - Technical Debt & Backlog (BACKLOG.md)

> **【運用ルール】**
> このファイルは、将来解消すべき課題（技術的負債）や未実装のタスクを記録する場所である。
> AIエージェントは、POからの明確な指示がない限り、通常の機能実装時にこのファイルを参照・解決しようとしてはならない（コンテキストの混入を防ぐため）。

## UI / UX & ルーティング関連
- [ ] **App.tsx のナビゲーション統合**: Inception DeckとKanbanのヘッダーデザインを共通化し、一貫したグローバルナビゲーション（設定アイコン、プロジェクト選択等）を提供するとともに、シームレスな画面遷移を実現する。
- [ ] **Inception Deckへの再入線UI欠如**: カンバン画面等から「インセプションデッキを再度見直す・修正する」ための明確なUI動線（ルーティング）が存在しない。

## AI連携 & LLM挙動の差異
- [ ] **AIタスク生成時のJSONパース正規表現の脆弱性**: `generate_tasks_from_story` 内のJSON抽出処理が最短マッチの正規表現 (`(?s)\[.*?\]`) で行われており、ネストされた配列などでパース失敗の可能性がある。将来的に Structured Output (`application/json`) の利用を検討。
- [ ] **AI呼び出しコスト(予算・トークン)の未計測**: コスト管理実現のため、AI呼び出しごとの usage(input/output tokens) を記録し、チャット単位でリアルタイムにコスト使用量を画面上で確認できるUIを実装する。

## Inception Deck (スプリント0) 関連
- [ ] **Inception Deckフェーズ完了後の自動タブ切り替え**: ドキュメント（差分）生成時に、対応するプレビュータブ（CONTEXT / ARCHITECTURE / RULE）を自動でアクティブにするUX改善。
- [ ] **生成ドキュメントのユーザー直接編集機能**: 右ペインのMarkdown表示をエディタに切り替え可能にし、AIが生成した内容をユーザーが手動で最終調整できるようにする。
- [ ] **フェーズ移行のUI化未済**: 次のフェーズへ進む際、現在はAIの応答に依存している。「次のフェーズへ進む」ボタン等によって明確に状態遷移をトリガーできるUI・制御ロジックを実装する。

## アーキテクチャ & 状態管理・安全性
- [ ] **タイマー稼働中の新規プロジェクト作成ガード漏れ**: スプリントタイマーが稼働中（RUNNING）にヘッダーから新規プロジェクトを作成すると、Interaction Guardが発動せず状態が引き継がれてしまう。レアケースとして現状許容中。
- [ ] **CRUD操作時の全件再フェッチ**: ストーリー等の追加・更新時に現状は `fetchStories()` 等でリストを全件再取得している。データ量増加時の遅延・チラつき防止のため、楽観的UI (Optimistic UI) への段階的移行を検討する。
- [ ] **TauriコマンドのContext未公開**: `update_project` や `delete_project` が WorkspaceContext 経由で呼び出せるようになっていないため、呼び出し経路が統一されていない箇所がある。
- [ ] **未使用コード・デバッグログの残留**: Tauriテンプレート由来の `greet` コマンドや、`db.rs` 内の `println!` が残っている。リリース前にクリーンアップする。

- [ ] **ペインのリサイズ機能**: 左ペイン・右ペイン間、および左ペイン上下の境界線をドラッグでリサイズできる機能（`react-split`などの導入を検討）。
- [ ] **Terminal DockのトグルボタンのUI改善**: クリック領域の拡大、アイコンの視認性向上。

---

## 解決済み (Resolved in Epic 23)
- [x] **プロジェクト削除機能のUI実装**: 設定画面（Danger Zone）からプロジェクトを安全に削除できるUIの実装。
- [x] **設定ボタンのアクセシビリティ改善**: グローバル設定モーダルの導入によるAI設定とプロジェクト管理の統合。
- [x] **AIモデルバージョンの動的取得**: モデル名のハードコードを廃止し、APIから一覧をフェッチして選択・保存できる仕組みを実装。
- [x] **Claudeの前置きテキスト対策（JSONパース堅牢化）**: Markdownコードフェンスの除去とGreedyマッチによる、LLM応答からの確実なJSON抽出処理を実装。
- [x] **ドキュメント全上書きバグの解消**: 差分追記（patch_content）アーキテクチャの導入により、既存内容を保護しつつトークン効率の良い更新を実現。
- [x] **Inception Deck 履歴の永続化**: Tauri Storeを用いたチャット履歴とフェーズ状態の保存・復元。
- [x] **WorkspaceContext の初期化フォールバック**: 不正なProjectIdのガードと、削除後の自動切り替えロジックの実装。
- [x] **旧AI連携ボタンの完全削除**: `StorySwimlane`, `BacklogView` 等から不要になった旧ボタン・コンポーネント、および `SettingsModal.tsx` を削除しUIをクリーンアップ。
- [x] **Team Leader の MaxTurnError 修正**: エージェントの最大ターン数を拡張し、ツール実行後のレポート応答を確実に生成可能にした。